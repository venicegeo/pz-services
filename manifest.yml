applications:
- name: pz-services
  host: pz-services
  path: app.jar
  memory: 512M
  instances: 1
  timeout: 180
  buildpack: java_buildpack_offline
  services:
    - pz-ping
    - pz-postgres
    - pz-geoserver
    - pz-blobstore
  env:
    CF_INSTANCE_PORTS: '[{external:5432,internal:5432}]'
